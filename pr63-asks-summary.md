Hynek's comment has three concrete asks:

1. Verify Google OAuth still works after the `oauthUtils.ts` change.
2. Decide whether to deprecate/remove the old Notion integration.
3. Expand `NotionMcp.info` with a clear description and a link to MCP HTTP docs.
